#include <stdio.h>
#include <math.h>
int main()
{
    float s = 0, n, x;
    int i, c = 1;
    do
    {
        scanf("%f%f", &n, &x);
    } while (n >= 30);
    {
        for (i; i <= 30; n++)
        {
            s = c * ((n * x) / (n + x))
                        c = -1;
        }
    }
}